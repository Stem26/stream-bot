/**
 * Feature flags для управления функциональностью бота
 * 
 * ENABLE_BOT_FEATURES - управляет сообщениями в чат (welcome, follow, announcements):
 * - true  = Сообщения отправляются
 * - false = Только логи и мониторинг
 * 
 * ALLOW_LOCAL_COMMANDS - дополнительная защита от дублирования команд:
 * - true  = Локально команды РАБОТАЮТ (для теста, ⚠️ отключи на сервере!)
 * - false = Локально команды БЛОКИРОВАНЫ (по умолчанию, безопасно)
 * 
 * НАСТРОЙКА (.env.local):
 * 
 * 🔍 Наблюдение (по умолчанию):
 *    ENABLE_BOT_FEATURES=false
 *    # ALLOW_LOCAL_COMMANDS не нужен
 *    → Только логи, ничего не дублируется ✅
 * 
 * 🧪 Тест сообщений (welcome, announcements):
 *    ENABLE_BOT_FEATURES=true
 *    # ALLOW_LOCAL_COMMANDS не нужен
 *    → Сообщения работают, команды блокированы ✅
 * 
 * ⚠️ Тест команд (ОПАСНО - нужно отключить на сервере!):
 *    ENABLE_BOT_FEATURES=true
 *    ALLOW_LOCAL_COMMANDS=true
 *    → ВСЁ работает локально (убедись что на сервере ENABLE_BOT_FEATURES=false!)
 */

/**
 * Управление сообщениями в чат
 * По умолчанию true, если не задано
 */
export const ENABLE_BOT_FEATURES = process.env.ENABLE_BOT_FEATURES !== 'false';

/**
 * Разрешить выполнение команд локально (дополнительная защита)
 * По умолчанию false для безопасности
 */
export const ALLOW_LOCAL_COMMANDS = process.env.ALLOW_LOCAL_COMMANDS === 'true';
