/**
 * Feature flags для управления функциональностью бота
 * Этот файл находится в git, поэтому изменения синхронизируются между окружениями
 * 
 * ГЛАВНЫЙ ФЛАГ:
 * - true = ВСЕ функции бота работают (продакшен)
 * - false = ВСЕ функции отключены, только логи (локальная разработка)
 */

/**
 * Главный переключатель всех функций бота
 * true - продакшен режим (все работает)
 * false - dev режим (только логи)
 */
export const ENABLE_BOT_FEATURES = true;
